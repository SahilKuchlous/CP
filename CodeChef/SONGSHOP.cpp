#include <iostream>
using namespace std;
int main () {
	int n, m, p;
	cin >> n >> m >> p;
	int songs[n][3];
	for (int i=0; i<n; i++) {
		cin >> songs[n][0] >> songs[n][1] >> songs[n][2] >> endl;
	}
	int albums[m];
	for (int i=0; i<m; i++) {
		cin >> albums[i];
	}
}