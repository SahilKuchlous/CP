#include <iostream>
using namespace std;
#define MAXN 1000005
int n;
char a[MAXN];
int sz[MAXN];
int pre0[MAXN], pre1[MAXN];
int suf0[MAXN], suf1[MAXN];
int main () {
	ios_base:sync_with_stdio(false);
	cin.tie(0); cout.tie(0);
	cin >> n;
	for (int i=0; i<n; i++) {
		cin >> a[i];
	}
	
}